<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="emp">


<!-- 업무목록조회 -->
<select id="jobs">
	select job_id
	from employees
</select>

<!-- 사원정보저장 -->
<insert id="insert">
	insert into employees(department_id, company_cd, name, email, phone, admin, job_id, position, employee_pattern)
				values(#{department_id}, #{company_cd}, #{name}, #{email}, #{phone}
				, #{admin}, #{jbo_id}, #{position}, #{employee_pattern})
</insert>

<!-- 부서목록조회 -->
<select id="departments" resultType="emp.DepartmentVO">
	SELECT department_id, department_name
	FROM departments
	order by department_id
</select>

<!-- 사원목록조회 -->
<select id="empList" resultType="emp.EmpVO">
	SELECT *
	FROM employees
</select>

<!-- 회사코드목록 -->
<select id="company" resultType="emp.CompanyVO">
	SELECT company_cd, company_name, comany_tel, comany_addr
	FROM company
</select>

</mapper>